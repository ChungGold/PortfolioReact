import SkillCard from './SkillCard'

const Skills = props => {
    return (<section id = 'skillset'>
        <SkillCard />
        <SkillCard />
        <SkillCard />
    </section>);
}

export default Skills;